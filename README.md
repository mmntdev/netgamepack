# NET GAME PACK 🕹️

ブラウザだけで、みんなで遊べるオンラインミニゲーム集。
トップページでゲームを選び、4文字のルームコード(または招待リンク)を友だちに共有して一緒に遊べます。

## 収録ゲーム

| ゲーム | 人数 | 内容 |
| --- | --- | --- |
| 🧱 みんなでブロック崩し | 1〜4人(協力) | ライフ共有・スコア個人戦。パワーアップ(マルチボール / パドル拡大 / スロー / 残機+1)つき。途中参加OK |
| 🛡️ エッジ・ディフェンス | 1〜6人(協力) | 正n角形の1辺をひとり1辺ずつ担当。自分の辺を抜かれると共有ライフが減り、誰もいない辺は壁が守る。人数でアリーナが変化(1人=三角形 / 2人=正方形の上下 / 3〜6人=人数と同じ角数、6人で六角形)。退出者の辺は壁になる |
| 🔷 ポリゴン・ブロック崩し | 1〜4人(協力) | 中央のブロック群を全方位から崩す。正n角形アリーナの外周をパドルが周回し、どこからボールが抜けてもライフを失う。レベルごとに角数が変化(六→五→四→八→三角形) |
| 🍳 クレイジーキッチン | 1〜4人(協力) | オーバークック風。食材を取る→切る→煮込む→盛り付け→提供の分業クッキング。レシピはサラダ/トマトサラダ/オニオンスープ。鍋は放置すると焦げる。3分間のチームスコア制・途中参加OK |
| ⚔️ キッチンバトル 2vs2 | 2〜4人(チーム対戦) | 左右対称のキッチンに分かれるチーム対抗クッキング。注文は共有キューで早い者勝ち、先に提供したチームが得点。中央カウンターは共用なので食材の取り合いも!3分間の合計スコア勝負 |
| 🐍 マルチスネーク | 1〜6人(対戦) | 2分間のスコアマッチ。エサ+10点、相手を自分の体に突っ込ませると+30点。倒された蛇はエサになり、2.5秒で復活(直後は無敵)。途中参加OK |
| 🏓 対戦 PONG | 2人(対戦) | 先に7点取ったほうが勝ち。3人目以降は観戦モード |

サーバー権威(server-authoritative)方式で、物理演算はすべてサーバー(60Hz)で行い、
クライアントには 20Hz でスナップショットを配信して補間描画します。

## ローカルで動かす

```bash
npm install
npm start
# → http://localhost:3000
```

テスト(サーバーを起動して2クライアントで実際にプレイするスモークテスト):

```bash
npm install          # devDependencies も入れる
npm test
```

## Render にデプロイする

このリポジトリは [Render](https://render.com) の Blueprint(`render.yaml`)に対応しています。

### 方法1: Blueprint(推奨)

1. このリポジトリを自分の GitHub にプッシュ
2. Render ダッシュボード → **New +** → **Blueprint**
3. リポジトリを選択すると `render.yaml` が読み込まれ、Web Service が自動作成される
4. デプロイ完了後に発行される URL にアクセス

### 方法2: 手動で Web Service を作成

1. Render ダッシュボード → **New +** → **Web Service** → リポジトリを選択
2. 設定:
   - **Runtime**: Node
   - **Build Command**: `npm ci --omit=dev`
   - **Start Command**: `npm start`
   - **Health Check Path**: `/healthz`
3. **Create Web Service** で完了

ポートは環境変数 `PORT` から自動で読み取ります(Render が自動設定)。
WebSocket(Socket.IO)は Render の Web Service でそのまま動作します。

> **無料プランの注意**: Free プランのサービスは15分間アクセスがないとスリープし、
> 次のアクセス時の起動に数十秒かかります。スリープするとルーム状態(メモリ上)は消えます。

## アーキテクチャ

```
server/
  index.js        # Express + Socket.IO エントリポイント(/healthz, /api/games, /api/stats)
  roomManager.js  # ルーム作成/参加/観戦/ホスト移譲/ゲームループ(60Hz tick, 20Hz 配信)
  games/
    index.js      # ゲームレジストリ
    breakout.js   # ブロック崩しのサーバーロジック
    edges.js      # エッジ・ディフェンスのサーバーロジック
    kitchen.js    # クレイジーキッチンのサーバーロジック
    kitchenbattle.js # キッチンバトル 2vs2 のサーバーロジック
    polygon.js    # ポリゴン・ブロック崩しのサーバーロジック
    snake.js      # マルチスネークのサーバーロジック
    pong.js       # PONG のサーバーロジック
public/
  index.html      # ゲーム選択画面
  css/style.css   # 共通スタイル
  js/lobby.js     # 共通クライアント(ルームUI・スナップショット補間・入力送信)
  breakout/       # ブロック崩しクライアント
  edges/          # エッジ・ディフェンスクライアント
  kitchen/        # クレイジーキッチンクライアント
  kitchenbattle/  # キッチンバトル 2vs2 クライアント
  polygon/        # ポリゴン・ブロック崩しクライアント
  snake/          # マルチスネーククライアント
  pong/           # PONG クライアント
```

## 効果音

Web Audio API でその場で合成する効果音つき(音声ファイル不要)。パドル反射・ブロック破壊・
パワーアップ取得・調理の包丁/完成のチン♪/焦げ・提供のファンファーレ・スネークの捕食など、
主要なイベントで鳴ります。ゲーム画面右上の 🔊/🔇 ボタンでいつでもオフにできます(設定は保存)。
クライアントがスナップショットの差分からイベントを検出する方式のため、サーバー負荷はゼロです。

## CPUプレイヤー

すべてのゲームで、ルーム待機画面からホストが **🤖 CPUを追加 / 削除** できます(最大人数まで)。
CPUは開始人数にも数えられるため、PONG やキッチンバトルなど対戦系のゲームも1人でテストできます。

- ブロック崩し系 / PONG: ボールを追跡(わずかな揺らぎつき)
- スネーク: 最寄りのエサへ向かう貪欲AI(1歩先の衝突は回避)
- キッチン系: 経路探索(BFS)+作業手順AIで **サラダ / トマトサラダ** を実際に調理して提供(スープは作りません)

人間が全員退室すると、CPUだけのルームは自動で破棄されます。

## 新しいゲームの追加方法

1. `server/games/<id>.js` を作成し、`{ meta, Game }` をエクスポートする
   - `meta`: `{ id, name, description, minPlayers, maxPlayers, allowJoinInProgress, path }`
   - `Game` クラス: `constructor(players)` / `addPlayer` / `removePlayer` /
     `handleInput(id, data)` / `tick(dt)` / `serialize()` / `finished` / `result` / `playerCount`
2. `server/games/index.js` のリストに追加
3. `public/<id>/` にクライアントページを作成(`js/lobby.js` の `NetGame.createClient()` を利用)

ゲーム選択画面は `/api/games` から自動生成されるため、上記だけで新ゲームが一覧に並びます。
